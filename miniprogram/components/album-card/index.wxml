<view wx:if="{{ item }}" class="album-card" bindtap="onTap">
  <image class="image" src="{{ item.image }}" />
  <view class="title">{{ item.title }}</view>
  <view class="description">{{ item.description }}</view>
</view>