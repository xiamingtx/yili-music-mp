<title-banner title="{{ title }}" moreLbale="{{ moreLabel }}" hideMore="{{ hideMore }}" />

<scroll-view style="width: 100%;" scroll-x>
  <view class="list" style="{{ 'width:' + (itemCount * itemWidth + 50) + 'rpx' }}">
    <slot></slot>
  </view>
</scroll-view>
